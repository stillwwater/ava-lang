
-- Test script
-- This code is useless and possibly semantically incorrect

constant :: 3.14159
value: int
a : [12] of int
aa : [] of int
aaa :: [12] of int

main :: (args: [] int, num: int) -> int do
    print(args[0])
    x: int
    args[args.count - 1] = 67 + 76 * 2
    implicit := 1

    implicit_array := (1, 2, 3, 4, 5)

    x = x * 2 + 1
    x = 1 * 2 + 1
    x = 2 + (-args[x])

    f :: (n: int) do
        whatever()
    end

    if (x == 0) and (args[x] >= 2) then
        do
            y : int = 4 -- y undefined outside of block
            print(y + x)
        end
        print(x)
    elsif x < 0 then
        print("x is less than 0")
    else
        print(x)
    end

    c : char
    c = #char "a"

    v : str = "hello
    world"

    while x + 2 < 10 do
        print("Hello world!")
        x = x + 1
        break
    end

    return 0
end
